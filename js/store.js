/* ============================================================
   store.js — wadah data global.
   File ini HARUS dimuat sebelum semua file di /data.
   Jangan taruh materi di sini; materi ada di folder /data.
   ============================================================ */

/** Daftar kategori (diisi oleh data/kategori.js) */
const KATEGORI = [];

/** Daftar semua topik (diisi oleh tiap file di /data) */
const TOPICS = [];

/** Daftar istilah glosarium (diisi oleh data/glosarium.js) */
const GLOSARIUM = [];
